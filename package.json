{
    "name": "workers",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "type-check": "tsc",
        "typegen": "wrangler types",
        "format": "prettier --write '**/*.{ts,json,jsonc,yml,yaml}'",
        "format:check": "prettier --check '**/*.{ts,json,jsonc,yml,yaml}'",
        "deploy": "wrangler deploy --env dev"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devEngines": {
        "packageManager": {
            "name": "pnpm",
            "version": "^11.3.0",
            "onFail": "download"
        }
    },
    "type": "module",
    "devDependencies": {
        "@types/node": "^26.1.1",
        "prettier": "^3.9.6",
        "typescript": "^7.0.2",
        "wrangler": "^4.114.0"
    }
}
